apiVersion: v1
kind: Service
metadata:
  name: ms-user-service
  namespace: hospital
  labels:
    app: ms-user
spec:
  selector:
    app: ms-user
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
