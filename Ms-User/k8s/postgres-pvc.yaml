apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms-user-postgres-pvc
  namespace: hospital
  labels:
    app: ms-user-postgres
    tier: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
