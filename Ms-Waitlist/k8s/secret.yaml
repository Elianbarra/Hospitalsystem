apiVersion: v1
kind: Secret
metadata:
  name: ms-waitlist-secret
  namespace: hospital
type: Opaque
stringData:
  # Ms-Waitlist usa almacenamiento en memoria — no requiere credenciales de BD.
  # Agrega aquí variables sensibles adicionales si se necesitan en el futuro.
  PLACEHOLDER: "replace-if-needed"
