# =============================================================================
# MS-WAITLIST — Variables de entorno
# Copia este archivo como .env y completa con tus valores reales.
# NUNCA subas el .env al repositorio.
# =============================================================================

# URL interna de MS-Auth para descargar la JWKS (validación JWT)
# En K8s usa el nombre del Service: http://ms-auth-svc:8080
MS_AUTH_URL=http://localhost:8080
