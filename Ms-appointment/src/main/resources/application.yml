server:
  port: 8082

spring:
  application:
    name: ms-appointment

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  security:
    oauth2:
      resourceserver:
        jwt:
          # Descarga la public key de ms-auth una vez y valida tokens localmente
          jwk-set-uri: ${MS_AUTH_URL}/.well-known/jwks.json

ms-user:
  base-url: ${MS_USER_URL}

logging:
  level:
    com.hospital.msappointment: DEBUG
    org.flywaydb: INFO
