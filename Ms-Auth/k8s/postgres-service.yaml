apiVersion: v1
kind: Service
metadata:
  name: ms-auth-postgres-svc
  namespace: hospital
  labels:
    app: ms-auth-postgres
    tier: database
spec:
  selector:
    app: ms-auth-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
