# =============================================================================
# SECRETS — ms-auth
# IMPORTANTE: Reemplaza los valores antes de aplicar.
# No subas este archivo con secretos reales a Git.
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: ms-auth-db-secret
  namespace: hospital
  labels:
    app: ms-auth
    tier: database
type: Opaque
stringData:
  POSTGRES_USER:     "neondb_owner"
  POSTGRES_PASSWORD: "npg_mSvx5zk9NTHq"
  DB_URL:            "jdbc:postgresql://ep-patient-silence-ad1s4w9m-pooler.c-2.us-east-1.aws.neon.tech/msauth_db?sslmode=require"
